- if admin?
  %button{onclick:"delete_idea()", style: "cursor: pointer"}
    %h4{style: "color:red;"} Delete this Idea

:javascript
  function delete_idea() {
    if (confirm("Are you sure you want to delete #{idea.post}")) {
      window.location = window.location.protocol+"//"+window.location.host+"/idea/#{idea.uuid}/destroy"
    }
  }